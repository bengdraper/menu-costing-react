/*
// scratch pad working out sending data to express
send data from form (name number consent object)

const onAddData = (data) => {
    const newData = [ ...notesArray, data]
}

return (
    <addData onAddData={onAddData} />
)
*/